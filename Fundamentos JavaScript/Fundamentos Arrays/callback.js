const numerosAleatorios = [100,40,'jean'];

numerosAleatorios.forEach(exibe);

function exibe (itens){
    console.log(typeof(itens)); // TYPEOF() É USADO PARA SABER QUAL O TIPO, STRING,NUMBER OU BOOLEAN
}